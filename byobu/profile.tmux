source $BYOBU_PREFIX/share/byobu/profiles/tmux
set -g mouse on
# copy tmux selection into system clipboard
# on osx it requires: iterm2 Preferences → General → Selection → check "Applications in terminal may access clipboard"
set -g set-clipboard on
